# Dinotty Docker 环境变量

# 主机映射端口
DINOTTY_PORT=8999

# 认证 Token（留空则自动生成）
DINOTTY_TOKEN=

# 日志级别
RUST_LOG=info

# 时区
TZ=UTC

# 工作区目录（挂载到容器 /workspace）
WORKSPACE_DIR=./workspace

# Set to true only when every browser request reaches Dinotty through HTTPS.
# The production Compose override enables this automatically.
DINOTTY_COOKIE_SECURE=false
